---
# roles/frigate/handlers/main.yml

- name: restart frigate
  service:
    name: "{{ frigate_container_name }}"
    state: restarted
    daemon_reload: true

- name: restart podman
  service:
    name: podman
    state: restarted