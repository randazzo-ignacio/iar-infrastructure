# {{ ansible_managed }}
# Calendar backup timer -- runs daily at 3:00 AM.
# Check logs with: journalctl -u radicale-backup.service

[Unit]
Description=Daily Radicale calendar backup to git

[Timer]
OnCalendar=*-*-* 03:00:00
Persistent=true

[Install]
WantedBy=timers.target