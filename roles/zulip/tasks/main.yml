---
# roles/zulip/tasks/main.yml
# Deploys Zulip (self-hosted chat) via Podman Compose on sophon.
# Caddy on rammstein terminates TLS and reverse-proxies over WireGuard.
#
# Zulip needs: PostgreSQL, Memcached, RabbitMQ, Redis, and the Zulip server.
# The official Docker image bundles all of these as separate compose services.
#
# We use podman compose (docker-compose v2 as provider) following the same
# pattern as SecPlatform: system podman socket, SELinux :z labels on volumes.
#
# NOTE: docker-compose v2.5.1 does not support 'environment' in secrets
# definitions or 'attach'/'app_protocol' in service definitions.
# We write secrets to individual files (no trailing newline) and reference
# them with 'file:'. All config is in a single compose.yml (no override file).

# -- Validate required variables --
- name: Fail if zulip_postgres_password is not set
  fail:
    msg: "zulip_postgres_password must be set in vault"
  when: zulip_postgres_password | length == 0

- name: Fail if zulip_secret_key is not set
  fail:
    msg: "zulip_secret_key must be set in vault"
  when: zulip_secret_key | length == 0

- name: Fail if zulip_external_host is not set
  fail:
    msg: "zulip_external_host must be set in host_vars"
  when: zulip_external_host | length == 0

# -- Install docker-compose v2 if not present (same as SecPlatform) --
- name: Check if docker-compose v2 is installed
  command: "docker-compose version --short"
  register: zulip_dc_check
  changed_when: false
  failed_when: false

- name: Install docker-compose v2
  get_url:
    url: "https://github.com/docker/compose/releases/download/v{{ zulip_compose_version | default('2.5.1') }}/docker-compose-linux-{{ ansible_architecture }}"
    dest: "/usr/local/bin/docker-compose"
    mode: '0755'
    owner: root
    group: root
  when: zulip_dc_check.rc != 0

# -- Enable podman socket (system) --
- name: Enable system podman socket
  systemd:
    name: podman.socket
    state: started
    enabled: true

# -- Create directories --
- name: Create Zulip compose directory
  file:
    path: "{{ zulip_compose_dir }}"
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Create Zulip data directories
  file:
    path: "{{ zulip_data_dir }}/{{ item.path }}"
    state: directory
    owner: "{{ item.owner }}"
    group: "{{ item.group }}"
    mode: '0755'
  loop:
    - { path: zulip, owner: root, group: root }
    - { path: postgresql-14, owner: root, group: root }
    - { path: rabbitmq, owner: root, group: root }
    - { path: redis, owner: "{{ zulip_redis_uid | string }}", group: "{{ zulip_redis_gid | string }}" }

# -- Write secret files (no trailing newline -- docker secrets must not have one) --
- name: Write Zulip secret files
  shell: "printf '%s' '{{ item.value }}' > {{ zulip_compose_dir }}/.env-{{ item.key }} && chmod 644 {{ zulip_compose_dir }}/.env-{{ item.key }}"
  loop:
    - { key: "postgres", value: "{{ zulip_postgres_password }}" }
    - { key: "memcached", value: "{{ zulip_memcached_password }}" }
    - { key: "rabbitmq", value: "{{ zulip_rabbitmq_password }}" }
    - { key: "redis", value: "{{ zulip_redis_password }}" }
    - { key: "secret-key", value: "{{ zulip_secret_key }}" }
    - { key: "email", value: "{{ zulip_email_password | default('') }}" }
  no_log: true
  notify: restart zulip

# -- Deploy compose.yml (single file, all config + secrets) --
- name: Deploy Zulip compose.yml
  template:
    src: compose.yml.j2
    dest: "{{ zulip_compose_dir }}/compose.yml"
    owner: root
    group: root
    mode: '0644'
  notify: restart zulip

- name: Deploy Zulip compose.override.yml
  template:
    src: compose.override.yml.j2
    dest: "{{ zulip_compose_dir }}/compose.override.yml"
    owner: root
    group: root
    mode: '0644'
  notify: restart zulip

# -- Create systemd service --
- name: Deploy Zulip systemd service
  template:
    src: zulip-stack.service.j2
    dest: /etc/systemd/system/zulip-stack.service
    owner: root
    group: root
    mode: '0644'
  notify: restart zulip

# -- Enable and start --
- name: Enable and start Zulip stack
  service:
    name: zulip-stack
    state: started
    enabled: true

# -- Wait for Zulip to be ready --
- name: Wait for Zulip to be ready
  # Zulip 12 removed /__heartbeat (404). /api/v1/server_settings is the
  # documented readiness endpoint -- returns 200 once the app is serving.
  uri:
    url: "http://127.0.0.1:{{ zulip_http_port }}/api/v1/server_settings"
    method: GET
    status_code: [200]
  register: zulip_ready
  retries: 30
  delay: 10
  until: zulip_ready.status == 200
  changed_when: false