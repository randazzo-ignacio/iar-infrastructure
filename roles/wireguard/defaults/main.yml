---
# roles/wireguard/defaults/main.yml
# WireGuard defaults — overridden by group_vars/all.yml in practice.
# Kept here for role self-documentation and standalone usability.

wg_interface: wg0
wg_port: 51820
wg_network: "10.66.0.0/24"
wg_dns: "10.66.0.1"
wg_keepalive: 25
wg_mtu: 1420