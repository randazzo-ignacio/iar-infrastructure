ansible_user: daft
wg_ip: "10.66.0.3"
wg_endpoint: "i.ar"

# Caddy sites
caddy_sites:
  - domain: "i.ar"
    type: static
    root: "/var/www/emacboros"
  - domain: "grafana.i.ar"
    type: proxy
    backend_host: daftpunk
    backend_port: 3000

# Ollama (CPU-only, 16 cores, 64GB RAM)
ollama_enabled: true
ollama_gpu: false
ollama_models:
  - "llama3.3:70b"
  - "north-mini-code-1.0:q8_0"
ollama_listen: "10.66.0.3"

# Static site
static_page_root: "/var/www/emacboros"
tool_static_page_enabled: true

# Monitoring
monitoring_enabled: true