wg_ip: "10.66.0.3"
wg_endpoint: "i.ar"  # public endpoint for WireGuard

# Ollama configuration — 16 cores, 64GB RAM (CPU-only)
ollama_enabled: true
ollama_models:
  - "llama3.3:70b"
  - "north-mini-code-1.0:q8_0"
ollama_listen: "10.66.0.3"  # bind to WireGuard IP only

# Static web page — emacboros landing page served by Caddy file_server
tool_static_page_enabled: true
