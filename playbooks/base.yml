---
# playbooks/base.yml -- Base hardening only (no WireGuard).
# Usage: ansible-playbook playbooks/base.yml --ask-vault-pass

- name: Base configuration
  hosts: all
  become: true
  roles:
    - role: base
      tags: [base]