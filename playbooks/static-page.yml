---
# playbooks/static-page.yml — Deploy only the emacboros landing page.
# Usage: ansible-playbook playbooks/static-page.yml --vault-password-file .vault_pass
# Usage (with verbose): ansible-playbook playbooks/static-page.yml --vault-password-file .vault_pass -vvv

- name: i.ar — deploy static page content
  hosts: daftpunk
  roles:
    - role: static-page
      when: tool_static_page_enabled | default(false) | bool